services:
  - type: web
    name: ads-dashboard
    runtime: docker
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: META_ACCESS_TOKEN
        sync: false
      - key: META_AD_ACCOUNT_ID
        sync: false
      - key: META_API_VERSION
        value: v21.0
